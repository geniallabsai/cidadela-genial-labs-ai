# Copie para .env (nunca commitar .env) — segredos de produção vivem em vault/KMS
DATABASE_URL=postgres://usuario:senha@localhost:5432/projeto
JWT_SECRET=troque-por-um-segredo-forte
PORT={{PORTA_DEV}}
